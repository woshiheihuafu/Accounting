---
description: 子 Agent 路由策略，定义主 Agent 何时应将任务派发给哪个子 Agent
alwaysApply: true
---

# 子 Agent 路由策略

**主 Agent 是协调者，不是执行者。默认假设：任何可独立执行的任务都必须通过 Task 工具派发给子 Agent，由主 Agent 亲自执行是例外，需要明确理由。**

## 强制门禁（每次动手前执行）

在做出任何"实际执行"动作前（编辑/创建文件、运行 shell/git 命令、写代码、设计架构、写测试、跑大范围搜索做实现），**必须先停下来走一遍路由判断**：

1. 这个动作属于路由表里的哪一类？→ 命中即派发，不要自己做。
2. 不属于任何一类，或跨多类需要协调？→ 才由主 Agent 处理（见白名单）。
3. 不确定属于哪类，但像是某个子 Agent 的活？→ **派发**（默认派发优先于亲自执行）。

只有在确认动作落在下面的"主 Agent 白名单"内时，才允许直接执行。

## 路由表

| 任务类型 | subagent_type | 典型场景 / 触发词 |
| --- | --- | --- |
| Git 操作、Shell 命令、文件整理、环境配置、常识问答、临时脚本 | `misc` | git commit/branch/merge/rebase、运行脚本、重命名/移动文件、装依赖 |
| Android 技术方案、模块架构设计、依赖审查、技术风险评估 | `android-architect` | 从 PRD 生成整体/模块技术方案、设计模块边界、审查架构合理性 |
| Android 代码实现（Compose/ViewModel/Repository/网络层/Bug 修复） | `android-implementer` | 写/改 Kotlin/Compose、实现页面、修 Android bug、接网络请求 |
| 代码审查 | `code-reviewer` | 模块实现完成后审查、PR 前审查、跨模块变更审查 |
| 产品一致性审查 | `product-reviewer` | PRD 一致性检查、设计稿与需求对齐、需求冲突检测 |
| 测试生成与运行 | `test-runner` | ViewModel/Repository/Compose UI 测试 |
| Gradle 模块、CI/CD、lint/format 配置、依赖管理 | `devops` | 新增 Gradle 模块、配置 GitHub Actions、管理 `libs.versions.toml` |
| 产品/设计变更落地（改 PRD、`.pen`、需求/设计索引） | `product-reviewer`（审查）+ 主 Agent（编写） | 改产品规则/视觉规则、设计还原判定为"变更"后更新 spec |

> 问题分类门 / 设计还原 triage（判定问题属于 bug / 变更 / 遗留 + 登记 `intake.md`）属于**主 Agent 协调**（见白名单），遵循 `android-feature-workflow` 场景 G：证据采集（只读对照 PRD/设计/代码/运行）派 `explore`，判定后按分类再派对应 Agent（bug → `android-implementer`；变更 → 先 Product 域，再 `android-architect`；遗留 → 记录后按需）。

## 主 Agent 白名单（仅这些可以亲自做）

- 路由判断本身，以及向用户解释将派发给谁。
- 与用户的对话：澄清需求、确认决策、征求方案选择。
- **为决定"派发给谁"而做的轻量探查**：读少量文件、看目录结构。这只能用于做路由决策，不能顺手把实际任务也做了。
- 协调多个子 Agent：拆分任务、并行派发、汇总和复核各子 Agent 的产出。
- 子 Agent 明确回报受阻、需要主 Agent 决策或跨域协调时的处理。

## 反合理化红旗清单（出现以下念头 = 你正在违规，应改为派发）

| 心里冒出的念头 | 实际应对 |
| --- | --- |
| "这个很简单，我自己做更快" | 简单也派发。速度不是绕过路由的理由。 |
| "只是改一行 / 一个小文件" | 体量小不改变归属。命中路由表就派发。 |
| "我刚才已经读过这些文件了" | 读文件是路由探查，执行仍要派发。 |
| "先快速跑一下就知道结果" | 跑命令属 `misc`，写实现属对应 Agent，派发。 |
| "派发开销大，不如直接写" | 这是规则要求，不是性能优化建议。 |
| "顺手把这个也改了吧" | 顺手 = 越权执行。回到门禁重新判断。 |
| "这是连续任务的一部分" | 每个可独立执行的子任务单独走路由判断。 |
| "用户对设计/逻辑不满，我直接改代码或改视觉就好" | 这是**变更**，不是 bug。先走场景 G 分类门，变更必经 Product 域改 spec，再回写 Android 技术方案，不在代码里自定产品/视觉规则。 |
| "我发现个问题，先当 bug 直接修了" | 先走场景 G 判定是 bug / 变更 / 遗留，再决定路由与记录位置（`BUG-NNN` / Product spec / `LEFT-NNN`），并登记 `intake.md`。 |

## 其他规则

1. **组合任务**：一个请求含多个独立子任务时，按类别分别派发，可并行。
2. **Android 功能工作流**：涉及 PRD 到实现的完整流程时，遵循 `.cursor/skills/android-feature-workflow/SKILL.md` 的多 Agent 协作模式。
3. **问题分类门优先**：收到"对设计/逻辑不满、疑似 bug、设计还原"类请求时，先走 `android-feature-workflow` 场景 G 完成 bug/变更/遗留分类与 `intake.md` 登记，再按分类路由。严禁跳过分类直接改代码或改 spec。
4. **变更必经 Product**：任何会改动产品规则、视觉规则或 PRD/设计未定义内容的需求，必须先经 Product 域（`product-workflow` / `product-reviewer`）更新 spec，Android 侧不得自行拍板。
5. **可追溯**：派发时在 prompt 中给足上下文（子 Agent 看不到本轮对话历史），并在动手前用一句话说明"将派发给哪个子 Agent 做什么"。
